#include <stdio.h>

/**
 * main - main print the alphabet in reverse lowercase
 *
 * Return: 0 (successfully)
 *
 */

int main(void)
{
	char alpha = 'z';

	while (alpha >= 'a')
	{
		putchar(c);
		c--;
	}
	putchar('\n');

	return (0);
}

