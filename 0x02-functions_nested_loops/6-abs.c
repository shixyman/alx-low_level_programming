#include "main.h"

/**
 * _abs - make absoulte value givento fucntion
 * @number: argument of the function
 * Return: value 0 successefully
 */

int _abs(int number)
{
	if (number < 0)
	{
		return (n * (-1));
	}
	else
	{
		return (n);
	}
}
